import React from "react";
 
const ItemAccessories = (props) => {
    return (
        <div>
            <h4>{ props.title }</h4>
            <p>{ props.description }</p>
        </div>
    );
};
 
export default ItemAccessories;